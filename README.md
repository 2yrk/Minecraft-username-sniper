# Minecraft-username-sniper
a simple Minecraft username checker that checks for 4C usernanmes written in python
dependencies needed: python ofc and aiohttp to install run pip3 install aiohttp
to run the script simply move the usernames.txt file to the same folder as the sniper file and run :"python3 usernamesniper.py usernames.txt"
